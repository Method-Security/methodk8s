types:
  Address:
    properties:
      type: string
      address: string
  Node:
    properties:
      name: string
      arch: string
      os: string
      instancetype: string
      status: boolean
      clusterUrl: string
      addresses: list<Address>
  NodeReport:
    properties:
      nodes: optional<list<Node>>
      errors: optional<list<string>>


